{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Shell",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/app",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}/app"
        }
    ]
}